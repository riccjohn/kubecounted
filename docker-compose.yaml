services:
  redis:
    image: redis:alpine
    ports:
      - "6379:6379"

  express:
    depends_on:
    - redis
    environment:
      - REDIS_URL=redis://redis:6379
    ports:
      - "3000:3000"
    build: .
